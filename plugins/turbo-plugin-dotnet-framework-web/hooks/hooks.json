{
  "description": "turbo-plugin-dotnet-framework-web: PostToolUse EnterWorktree hook(advisory,Windows-only;目前為 no-op,保留 IIS Express applicationhost 處理的掛載點)。永不 block session。",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "EnterWorktree",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/hooks/invoke-posttooluse-enterworktree.sh\"",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
