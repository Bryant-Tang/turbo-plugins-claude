<!-- turbo-plugin:begin base -->
## 專案慣例（由 turbo-plugin 注入）

執行下列操作前,**先讀 `.turbo-plugin/conventions.md`** 並遵守其中指向的 skill:資料庫 / SQL 操作、撰寫 commit message、修改程式碼時的註解慣例(`*.cs` / `*.js` / `*.ts`)等。

**不得提交僅限本機才有的東西**:機器路徑、內部 hostname / URL、僅本機 / 單次情境識別碼(需求 / 計畫 / 任務代號、單一 session 項目編號),一律不得寫進版控(文件需舉例時改用 placeholder token)。
<!-- turbo-plugin:end base -->
